const { canonical, checksum, safeText } = require('./jefe-context-package-contract.cjs')
const { AGENT_PURPOSES } = require('./jefe-context-package-contract.cjs')
const FINDINGS = Object.freeze(['observation','evidence_reference','assumption','risk','question','requirement_proposal','constraint_proposal','plan_proposal','technical_result','validation_result','failure','correction_proposal'])
const FORBIDDEN = /human_decision|human_approval|visual_approval|deploy_approval|release_approval|published|production_ready|system.?prompt|chain.?of.?thought|reasoning|command|script|token|secret|cookie|header|environment|file:\/\//iu
const PATHLIKE = /(?:^|[\s"'])(?:[a-z]:[\\/]|\/(?:[\w.-]+[\\/])+|\.\.[\\/])/iu
class AgentResultError extends Error { constructor(code, message) { super(message); this.code=code } }
function fail(code,message){throw new AgentResultError(code,message)}
function text(value){return typeof value==='string'&&value.length<=2000&&safeText(value)&&!FORBIDDEN.test(value)&&!PATHLIKE.test(value)}
function reference(value){if(!value||typeof value!=='object'||Array.isArray(value)||Object.keys(value).some(key=>!['kind','value'].includes(key))||value.kind!=='url'||typeof value.value!=='string'||value.value.length>500||/[\x00-\x1f\x7f]/u.test(value.value)||FORBIDDEN.test(value.value))return null;let url;try{url=new URL(value.value)}catch{return null};const host=url.hostname.toLowerCase().replace(/^\[|\]$/gu,'');if(url.protocol!=='https:'||url.username||url.password||!host||host==='localhost'||host.endsWith('.localhost')||host==='::1'||host==='0.0.0.0'||/^127(?:\.\d{1,3}){3}$/u.test(host))return null;return {kind:'url',value:url.toString()}}
function validateResult(raw, attempt, now){
 if(!raw||typeof raw!=='object'||Array.isArray(raw)||Object.keys(raw).some(k=>!['status','summary','findings','artifactReferences','requestedHumanAction','diagnostics'].includes(k))||['artifactReferences','requestedHumanAction','diagnostics'].some(key=>Object.hasOwn(raw,key)&&raw[key]===undefined))fail('INVALID_RESULT','El resultado del consumidor no es valido.')
 if(!['success','partial','failed'].includes(raw.status)||!text(raw.summary)||!Array.isArray(raw.findings)||raw.findings.length>30)fail('INVALID_RESULT','El resultado del consumidor no es valido.')
 const findings=raw.findings.map(item=>{if(!item||typeof item!=='object'||Array.isArray(item)||Object.keys(item).some(key=>!['kind','summary','references'].includes(key))||(Object.hasOwn(item,'references')&&(!Array.isArray(item.references)||item.references.length>20))||!FINDINGS.includes(item.kind)||!text(item.summary)||!AGENT_PURPOSES[attempt.targetAgent])fail('INVALID_FINDING','El finding no es valido.');const references=(item.references||[]).map(reference);if(references.some(value=>!value))fail('INVALID_REFERENCE','La referencia no es valida.');return {kind:item.kind,summary:item.summary,references}})
 if(Object.hasOwn(raw,'artifactReferences')&&(!Array.isArray(raw.artifactReferences)||raw.artifactReferences.length>20))fail('INVALID_REFERENCE','La referencia no es valida.')
 const refs=(raw.artifactReferences||[]).map(reference);if(refs.some(value=>!value))fail('INVALID_REFERENCE','La referencia no es valida.')
 if(Object.hasOwn(raw,'requestedHumanAction')&&!text(raw.requestedHumanAction))fail('INVALID_RESULT','La accion humana solicitada no es valida.')
 if(Object.hasOwn(raw,'diagnostics')&&!text(raw.diagnostics))fail('INVALID_RESULT','El resultado del consumidor no es valido.')
 const data={schemaVersion:'jefe-agent-result/v1',attemptId:attempt.attemptId,packageId:attempt.packageId,targetAgent:attempt.targetAgent,purpose:attempt.purpose,identity:attempt.identity,actor:'internal_consumer',authority:'untrusted',provenance:'agent_context_handoff',status:raw.status,summary:raw.summary,findings,artifactReferences:refs,requestedHumanAction:raw.requestedHumanAction||null,diagnostics:raw.diagnostics&&text(raw.diagnostics)?raw.diagnostics:null,createdAt:now}
 const resultId=`agent-result-${checksum(data).slice(0,32)}`; return {...data,resultId,integrity:{algorithm:'sha256',checksum:checksum({...data,resultId}),deterministic:true,trust:'untrusted',ingestion:'not_ingested'}}
}
module.exports={AgentResultError,FINDINGS,validateResult}
