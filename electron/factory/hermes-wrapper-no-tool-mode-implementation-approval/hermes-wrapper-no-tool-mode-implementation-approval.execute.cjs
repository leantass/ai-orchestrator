const fs = require('node:fs/promises');
const path = require('node:path');
const { pathToFileURL } = require('node:url');
const { assertWrapperNoToolModeImplementationApprovalPathContained, resolveFactoryHermesWrapperNoToolModeImplementationApprovalPaths } = require('./hermes-wrapper-no-tool-mode-implementation-approval.path.cjs');
async function readJson(file) { const text = await fs.readFile(file, 'utf8'); if (!text.trim()) throw new Error(`json_file_empty: ${file}`); return JSON.parse(text); }
async function executeFactoryHermesWrapperNoToolModeImplementationApproval(input = {}) { const paths = resolveFactoryHermesWrapperNoToolModeImplementationApprovalPaths(); for (const [key, value] of Object.entries(paths)) if (!['repoRoot','installRoot'].includes(key)) assertWrapperNoToolModeImplementationApprovalPathContained(value, paths.installRoot); const moduleUrl = pathToFileURL(path.join(paths.repoRoot, 'src', 'factory', 'hermes-wrapper-no-tool-mode-implementation-approval', 'index.ts')).href; const { evaluateFactoryHermesWrapperNoToolModeImplementationApproval, serializeFactoryHermesWrapperNoToolModeImplementationApprovalResult } = await import(moduleUrl); const result = evaluateFactoryHermesWrapperNoToolModeImplementationApproval({ evaluatedAt: input.evaluatedAt || '2026-07-23T04:00:00.000Z', evaluatedBy: input.evaluatedBy || 'factory-hermes-wrapper-no-tool-mode-implementation-approval-smoke', implementationPlanningResult: input.implementationPlanningResult || await readJson(paths.implementationPlanningResult), wrapperNoToolModeApprovalResult: input.wrapperNoToolModeApprovalResult || await readJson(paths.wrapperApprovalResult), wrapperNoToolModePlanningResult: input.wrapperNoToolModePlanningResult || await readJson(paths.wrapperPlanningResult), runtimeSelectionRevisionPlanningResult: input.runtimeSelectionRevisionPlanningResult || await readJson(paths.revisionPlanningResult), toolsetDisableVerificationApprovalResult: input.toolsetDisableVerificationApprovalResult || await readJson(paths.toolsetApprovalResult), researchRuntimeAdapterApprovalResult: input.researchRuntimeAdapterApprovalResult || await readJson(paths.adapterApprovalResult), approvalNotes: input.approvalNotes }); await fs.mkdir(path.dirname(paths.implementationApprovalResult), { recursive: true }); await fs.writeFile(paths.implementationApprovalResult, serializeFactoryHermesWrapperNoToolModeImplementationApprovalResult(result)); return result; }
module.exports = { executeFactoryHermesWrapperNoToolModeImplementationApproval };
