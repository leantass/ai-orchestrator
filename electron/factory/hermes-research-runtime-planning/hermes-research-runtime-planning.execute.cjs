const fs = require('node:fs/promises');
const path = require('node:path');
const { resolveFactoryHermesResearchRuntimePlanningPaths, assertPlanningPathContained } = require('./hermes-research-runtime-planning.path.cjs');
const { inspectHermesResearchRuntimeSource } = require('./hermes-research-runtime-planning.inspect.cjs');

const KIND = 'factory-hermes-research-runtime-planning';
const VERSION = '1.0';
const NEXT = 'Proceed to Factory Hermes Research Runtime Boundary Contract v1; Hermes execution remains forbidden.';
const notAuthorizedActions = ['execute_hermes_now', 'run_hermes_scripts_now', 'execute_python_now', 'execute_uv_now', 'execute_uv_run_now', 'execute_uv_pip_now', 'execute_pip_now', 'execute_setup_py_now', 'call_models_now', 'access_credentials_now', 'use_network_now', 'scrape_now', 'execute_codex_now', 'deploy_now', 'mutate_project_files_now', 'create_runtime_now'];
const nextGates = ['Factory Hermes Research Runtime Boundary Contract v1', 'Factory Hermes Research Runtime Approval Gate v1', 'Factory Hermes Research Runtime Adapter v1', 'Factory Hermes Research Result Ingestion Gate v1', 'Factory Hermes Research JEFE Review Gate v1'];
async function readJson(file) { return JSON.parse(await fs.readFile(file, 'utf8')); }
function evaluate(input) { const r = input.pythonInstallJefeReviewResult; const s = input.sourceInspection; const id = `hermes-research-runtime-planning:75b300f:${input.plannedAt}`; const base = { planningId: id, planningKind: KIND, planningVersion: VERSION, plannedAt: input.plannedAt, plannedBy: input.plannedBy, toolId: 'hermes_agent', auditedHead: r?.auditedHead || '', sourceRootRef: r?.sourceRootRef || '', installRootRef: r?.installRootRef || '', pythonEnvRootRef: r?.pythonEnvRootRef || '', uvExecutableRef: r?.uvExecutableRef || '', pythonInstallJefeReviewRef: '.codex-temp/external-tools/hermes-agent/install/75b300f/python-install-jefe-review-result.json', pythonInstallVerificationRef: r?.pythonInstallVerificationRef || '', sourceInspectionSummary: s?.summary || {}, interfaceStatus: s?.interfaceStatus || 'missing', interfaceCandidates: s?.interfaceCandidates || [], checks: [], blockers: [], warnings: [], canExecuteHermes: false, canRunHermesScripts: false, canUseCredentials: false, canCallModels: false, canUseNetwork: false, canMutateProjectFiles: false, canDeploy: false, recommendedNextStep: NEXT }; if (!r) return { ...base, status: 'blocked', decision: 'blocked_missing_jefe_review', canProceedToResearchRuntimeBoundary: false, blockers: [{ blockerId: 'blocked_missing_jefe_review', message: 'JEFE Review is required.' }] }; if (r.status !== 'approved' || r.canProceedToResearchRuntimePlanning !== true || r.canExecuteHermes !== false) return { ...base, status: 'blocked', decision: 'blocked_jefe_review_not_approved_for_planning', canProceedToResearchRuntimeBoundary: false, blockers: [{ blockerId: 'blocked_jefe_review_not_approved_for_planning', message: 'JEFE Review is not approved for planning.' }] }; if (!s || s.interfaceStatus === 'blocked_source_missing') return { ...base, status: 'blocked', decision: 'blocked_source_missing', canProceedToResearchRuntimeBoundary: false, blockers: [{ blockerId: 'blocked_source_missing', message: 'Source missing.' }] }; if (s.pythonInstallVerified !== true || s.uvVerified !== true) return { ...base, status: 'blocked', decision: 'blocked_python_install_not_ready', canProceedToResearchRuntimeBoundary: false, blockers: [{ blockerId: 'blocked_python_install_not_ready', message: 'Python/UV readiness missing.' }] }; const canProceed = s.interfaceStatus === 'interface_candidate_identified'; const decision = canProceed ? 'hermes_research_runtime_plan_candidate_created' : s.interfaceStatus === 'interface_requires_manual_selection' ? 'hermes_research_runtime_plan_requires_manual_interface_selection' : 'request_source_interface_review'; const status = canProceed ? 'plan_candidate_created' : 'manual_selection_required'; const receipt = { receiptId: `${id}:receipt`, planningId: id, toolId: 'hermes_agent', auditedHead: r.auditedHead, plannedBy: input.plannedBy, plannedAt: input.plannedAt, decision, scope: 'hermes_research_runtime_planning_only', approvedNextGate: 'Factory Hermes Research Runtime Boundary Contract v1', limitations: ['Planning only.', 'No runtime execution.', 'No network, credentials or model calls.'], notAuthorizedActions }; const plan = { planCandidateId: `hermes-research-runtime-plan:75b300f:${input.plannedAt}`, toolId: 'hermes_agent', auditedHead: r.auditedHead, sourceRootRef: r.sourceRootRef, installRootRef: r.installRootRef, pythonEnvRootRef: r.pythonEnvRootRef, uvExecutableRef: r.uvExecutableRef, interfaceStatus: s.interfaceStatus, selectedInterfaceCandidate: canProceed && s.interfaceCandidates.length === 1 ? s.interfaceCandidates[0] : undefined, interfaceCandidates: s.interfaceCandidates, runtimeModeCandidate: 'hermes_research_runtime_controlled', proposedRuntimeBoundary: { allowedFilesystemRoots: [r.installRootRef], forbiddenFilesystemRoots: ['.env', 'node_modules', 'web-prueba'], workingDirectory: r.sourceRootRef, outputDirectory: `${r.installRootRef}/research-output/`, tempDirectory: `${r.installRootRef}/research-temp/`, cacheDirectory: `${r.installRootRef}/research-cache/` }, proposedCommandPolicy: { commandsAllowedNow: [], commandsAllowedInFutureRuntimeCandidate: s.interfaceCandidates.map((c) => c.commandTextCandidate).filter(Boolean), commandsForbidden: ['pip', 'setup.py', 'uv pip', 'uv run unrestricted', 'shell', 'cmd', 'powershell'] }, proposedNetworkPolicy: { networkAllowedNow: false, futureNetworkRequiresApproval: true, allowedDomainsFuture: [], blockedByDefault: true }, proposedCredentialPolicy: { credentialsAllowedNow: false, futureCredentialsRequireApproval: true, noEnvReadInPlanning: true }, proposedModelPolicy: { modelCallsAllowedNow: false, futureModelCallsRequireApproval: true }, proposedInputContract: { researchQuestion: 'required', constraints: 'required', allowedSources: [], forbiddenSources: [], maxRuntimeMs: 300000, outputSchema: 'structured_findings_v1' }, proposedOutputContract: { structuredFindings: true, citationsOrEvidence: true, confidence: true, warnings: true, rawLogsSanitized: true }, proposedSafety: { timeouts: true, killSwitches: true, logSanitization: true, resultIngestionRequired: true, jefeReviewRequired: true }, requiredNextGates: nextGates }; return { ...base, status, decision, researchRuntimePlanningReceipt: receipt, hermesResearchRuntimePlanCandidate: plan, canProceedToResearchRuntimeBoundary: canProceed }; }
async function executeFactoryHermesResearchRuntimePlanning(input) { const paths = resolveFactoryHermesResearchRuntimePlanningPaths(); assertPlanningPathContained(paths.installRoot, path.join(paths.root, '.codex-temp')); assertPlanningPathContained(paths.sourceRoot, paths.installRoot); const jefe = await readJson(paths.jefeReviewResult); const pyv = await readJson(paths.pythonVerificationResult); await readJson(paths.pythonInstallResult); const uvv = await readJson(paths.uvVerificationResult); const sourceInspection = await inspectHermesResearchRuntimeSource(paths, pyv, uvv); const result = evaluate({ ...input, pythonInstallJefeReviewResult: jefe, sourceInspection }); await fs.writeFile(paths.planningResult, JSON.stringify(result, null, 2)); return result; }
module.exports = { executeFactoryHermesResearchRuntimePlanning };
