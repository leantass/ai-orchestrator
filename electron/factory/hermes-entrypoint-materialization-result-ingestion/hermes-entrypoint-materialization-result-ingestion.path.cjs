const path = require('node:path');
function repoRoot() { return path.resolve(__dirname, '../../..'); }
function resolveFactoryHermesEntrypointMaterializationResultIngestionPaths(root = repoRoot()) { const installRoot = path.join(root, '.codex-temp/external-tools/hermes-agent/install/75b300f'); return { root, installRoot, runtimeResult: path.join(installRoot, 'entrypoint-materialization-runtime-result.json'), runtimeManifest: path.join(installRoot, 'entrypoint-materialization-runtime-manifest.json'), approvalResult: path.join(installRoot, 'entrypoint-materialization-approval-result.json'), planningResult: path.join(installRoot, 'entrypoint-materialization-planning-result.json'), researchJefeReviewResult: path.join(installRoot, 'research-jefe-review-result.json'), ingestionResult: path.join(installRoot, 'entrypoint-materialization-result-ingestion-result.json') } }
function assertResultIngestionPathContained(target, root) { const rel = path.relative(path.resolve(root), path.resolve(target)); if (rel.startsWith('..') || path.isAbsolute(rel)) throw new Error(`Path escapes root: ${target}`) }
module.exports = { resolveFactoryHermesEntrypointMaterializationResultIngestionPaths, assertResultIngestionPathContained };
