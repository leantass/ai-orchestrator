const path = require('node:path');
function resolveFactoryHermesEntrypointMaterializationPlanningPaths(root = process.cwd()) { const installRoot = path.join(root, '.codex-temp', 'external-tools', 'hermes-agent', 'install', '75b300f'); const sourceRoot = path.join(installRoot, 'source'); const pythonEnvRoot = path.join(installRoot, 'python-env'); return { root, installRoot, sourceRoot, pythonEnvRoot, pyproject: path.join(sourceRoot, 'pyproject.toml'), uvLock: path.join(sourceRoot, 'uv.lock'), setupPy: path.join(sourceRoot, 'setup.py'), setupCfg: path.join(sourceRoot, 'setup.cfg'), missingExecutable: path.join(pythonEnvRoot, 'Scripts', 'hermes.exe'), pythonExe: path.join(pythonEnvRoot, 'Scripts', 'python.exe'), pyvenvCfg: path.join(pythonEnvRoot, 'pyvenv.cfg'), sitePackages: path.join(pythonEnvRoot, 'Lib', 'site-packages'), jefeReviewResult: path.join(installRoot, 'research-jefe-review-result.json'), planningResult: path.join(installRoot, 'entrypoint-materialization-planning-result.json') } }
function assertPlanningPathContained(target, root) { const rel = path.relative(path.resolve(root), path.resolve(target)); if (rel.startsWith('..') || path.isAbsolute(rel)) throw new Error(`Path escapes allowed root: ${target}`) }
module.exports = { resolveFactoryHermesEntrypointMaterializationPlanningPaths, assertPlanningPathContained };
