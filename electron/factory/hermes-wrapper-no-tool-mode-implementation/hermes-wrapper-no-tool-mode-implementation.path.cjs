const path = require('node:path');
function resolveRepoRoot() { return path.resolve(__dirname, '..', '..', '..'); }
function assertWrapperNoToolModeImplementationPathContained(targetPath, rootPath) { const target = path.resolve(targetPath); const root = path.resolve(rootPath); const relative = path.relative(root, target); if (relative.startsWith('..') || path.isAbsolute(relative)) throw new Error(`path_outside_root: ${target}`); }
function resolveFactoryHermesWrapperNoToolModeImplementationPaths() { const repoRoot = resolveRepoRoot(); const installRoot = path.join(repoRoot, '.codex-temp', 'external-tools', 'hermes-agent', 'install', '75b300f'); return { repoRoot, installRoot, implementationApprovalResult: path.join(installRoot, 'wrapper-no-tool-mode-implementation-approval-result.json'), implementationPlanningResult: path.join(installRoot, 'wrapper-no-tool-mode-implementation-planning-result.json'), implementationResult: path.join(installRoot, 'wrapper-no-tool-mode-implementation-result.json'), runtimeSource: path.join(repoRoot, 'src', 'factory', 'hermes-wrapper-no-tool-mode-runtime', 'index.ts'), runtimeCjs: path.join(repoRoot, 'electron', 'factory', 'hermes-wrapper-no-tool-mode-runtime', 'index.cjs') }; }
module.exports = { assertWrapperNoToolModeImplementationPathContained, resolveFactoryHermesWrapperNoToolModeImplementationPaths };
