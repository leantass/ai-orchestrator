const crypto = require('crypto'); const { canonical, AGENT_PURPOSES } = require('./jefe-context-package-contract.cjs')
const { validateIntake } = require('./jefe-discovery-contract.cjs')
class DiscoveryOrchestratorError extends Error { constructor(code, message) { super(message); this.code = code } }
function fail(code, message) { throw new DiscoveryOrchestratorError(code, message) }
function createSupervisedDiscovery({ persistence, memory = null, contextService = null, clock = () => new Date().toISOString() } = {}) { if (!persistence || typeof persistence.save !== 'function') fail('INVALID_DEPENDENCY', 'Persistencia invalida.'); async function createIntake(raw) { const intake = validateIntake(raw, clock()); const saved = await persistence.save(intake); if (memory && intake.identity?.versionId) { const base = { scope: 'version', identity: intake.identity, actor: 'lean', authority: 'human_decision', provenance: 'supervised_discovery_intake', timestamp: intake.createdAt, references: intake.references.map(({ kind, value }) => ({ kind, value })), relations: [], metadata: { intakeId: intake.intakeId, state: intake.state, nextResponsible: intake.nextResponsible } }; await memory.append({ ...base, entryId: `intake-objective-${crypto.createHash('sha256').update(intake.intakeId).digest('hex').slice(0, 24)}`, type: 'objective', summary: intake.objective || 'Intake requiere aclaracion.' }); for (const [index, question] of intake.questions.entries()) await memory.append({ ...base, entryId: `intake-question-${crypto.createHash('sha256').update(`${intake.intakeId}:${index}`).digest('hex').slice(0, 24)}`, type: 'question', summary: question, metadata: { ...base.metadata, requiresLean: true } }) }
      let packages = []; if (contextService && intake.state === 'ready_for_discovery') { for (const agent of ['radar', 'hermes', 'scout']) { const prepared = await contextService.prepareAgentContext({ targetAgent: agent, purpose: AGENT_PURPOSES[agent], scope: 'version', identity: intake.identity, budget: { maxEntries: 20, maxCharacters: 8000 } }); packages.push({ agent, packageId: prepared.package.packageId, consumerStatus: prepared.handoff.consumerStatus }) } } return { intake: saved.record, idempotent: saved.idempotent, state: packages.length ? 'not_connected' : intake.state, packages } }
  async function reopen(intakeId) { const intake = await persistence.read(intakeId); if (!intake) fail('INTAKE_NOT_FOUND', 'Intake inexistente.'); return { intake, state: intake.state } }
  return { createIntake, reopen }
}
module.exports = { DiscoveryOrchestratorError, createSupervisedDiscovery }
