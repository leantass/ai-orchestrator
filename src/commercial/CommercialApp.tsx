import { useEffect, useState, type Dispatch, type SetStateAction } from 'react'
import { emptyDraft, eventLabel, loadDraft, payloadFromDraft, type Direction, type Draft, type Project, type Snapshot, type Version, type WorkspaceArea, validateStep, visibleProjects, workspaceView } from './hubModel'

type Result = { ok: boolean; error?: { message?: string }; projects?: Project[]; project?: Project; snapshot?: Snapshot; versions?: Version[]; comparison?: { artifacts: { added: string[]; removed: string[]; modified: string[] } }; preview?: PreviewState; approval?: ApprovalState; review?: { reviewId: string } }
type PreviewState = { previewRequestId: string; projectId: string; runId: string; versionId: string; resourceId: string; state: string; versionSnapshot: { snapshotSha256: string }; resource: { relativePath: string; mimeType: string } }
type ApprovalState = { approvalId: string; state: string; decision: string; reviewType?: string; terminalReason?: string; findings?: string[]; questions?: string[] }
type Bridge = { listProjects: () => Promise<Result>; getWorkspaceSnapshot: (id: string) => Promise<Result>; listVersions: (id: string) => Promise<Result>; createFirstVersion: (request: unknown) => Promise<Result>; createVersion: (id: string, change: string, options?: { visualDirection?: Direction }) => Promise<Result>; compareVersions: (id: string, left: string, right: string) => Promise<Result>; restoreVersion: (id: string, version: string) => Promise<Result>; prepareLocalDelivery: (id: string, version: string) => Promise<Result>; openPreview: (id: string) => Promise<Result>; openDelivery: (id: string, version?: string) => Promise<Result>; selectInputAssets: () => Promise<{ assets?: Array<{ safeName?: string; name?: string; kind?: string; size?: number }> }> }
const getBridge = () => (window as Window & { jefeProjectBridge?: Bridge }).jefeProjectBridge
const getPreviewBridge = () => (window as Window & { jefePreviewApprovalBridge?: { request: (payload: unknown) => Promise<Result>; readApproval: (projectId: string, previewRequestId: string) => Promise<Result>; review: (payload: unknown) => Promise<Result>; approval: (payload: unknown) => Promise<Result>; open: (projectId: string, previewRequestId: string) => Promise<Result> } }).jefePreviewApprovalBridge
const draftKey = 'jefe-commercial-draft-v1'; const themeKey = 'jefe-ui-theme'; const formatDate = (value: string | null | undefined) => value ? new Intl.DateTimeFormat('es-AR', { dateStyle: 'medium', timeStyle: 'short' }).format(new Date(value)) : 'Sin fecha registrada'
const types = [{ id: 'agency_site', label: 'Sitio web', glyph: '◫' }, { id: 'application', label: 'Aplicación', glyph: '⌘' }, { id: 'business_system', label: 'Sistema', glyph: '▦' }, { id: 'game', label: 'Juego', glyph: '✦' }, { id: 'other', label: 'Otro', glyph: '＋' }]

export function CommercialApp() {
  const [projects, setProjects] = useState<Project[]>([]); const [screen, setScreen] = useState<'home' | 'wizard' | 'workspace'>('home'); const [draft, setDraft] = useState<Draft>(() => loadDraft(localStorage.getItem(draftKey)) || emptyDraft()); const [snapshot, setSnapshot] = useState<Snapshot | null>(null); const [versions, setVersions] = useState<Version[]>([]); const [preview, setPreview] = useState<PreviewState | null>(null); const [approval, setApproval] = useState<ApprovalState | null>(null); const [area, setArea] = useState<WorkspaceArea>('summary'); const [notice, setNotice] = useState(''); const [busy, setBusy] = useState(false); const [theme, setTheme] = useState<'light' | 'dark'>(() => localStorage.getItem(themeKey) === 'dark' ? 'dark' : 'light')
  const refresh = async () => { const bridge = getBridge(); if (!bridge) { setNotice('JEFE Desktop es necesario para gestionar proyectos físicos.'); return }; const result = await bridge.listProjects(); if (!result.ok) { setNotice(result.error?.message || 'No se pudieron cargar los proyectos.'); return }; setProjects(result.projects || []) }
  // The initial bridge read synchronizes renderer state with the persisted local project registry.
  // eslint-disable-next-line react-hooks/set-state-in-effect
  useEffect(() => { void refresh() }, []); useEffect(() => { document.documentElement.dataset.theme = theme; localStorage.setItem(themeKey, theme) }, [theme])
  const openProject = async (projectId: string) => { const bridge = getBridge(); if (!bridge) return; setBusy(true); const [snapshotResult, versionsResult] = await Promise.all([bridge.getWorkspaceSnapshot(projectId), bridge.listVersions(projectId)]); if (!snapshotResult.ok || !snapshotResult.snapshot) { setNotice(snapshotResult.error?.message || 'No se pudo abrir el workspace.'); setBusy(false); return }; const nextSnapshot = snapshotResult.snapshot; const nextVersions = versionsResult.versions || []; setSnapshot(nextSnapshot); setVersions(nextVersions); const current = nextVersions.find((version) => version.versionId === nextSnapshot.activeVersionId) || nextVersions.at(-1); const pBridge = getPreviewBridge(); if (current?.runId && pBridge) { const requested = await pBridge.request({ projectId, runId: current.runId, versionId: current.versionId, resourceId: 'app-index' }); const nextPreview = requested.ok ? requested.preview || null : null; setPreview(nextPreview); setApproval(nextPreview ? ((await pBridge.readApproval(projectId, nextPreview.previewRequestId)).approval || null) : null) }; localStorage.setItem('jefe-active-project-id', projectId); setScreen('workspace'); setBusy(false) }
  const run = async (work: () => Promise<Result>, success: string) => { if (busy) return; setBusy(true); const result = await work(); setNotice(result.ok ? success : (result.error?.message || 'No se pudo completar la acción.')); if (result.ok && snapshot) await openProject(snapshot.projectId); setBusy(false) }
  const create = async () => { const invalid = [1, 2, 4].map((step) => validateStep(draft, step)).find(Boolean); if (invalid) { setNotice(invalid); return }; const bridge = getBridge(); if (!bridge) return; setBusy(true); const result = await bridge.createFirstVersion(payloadFromDraft(draft)); if (result.ok && result.project) { localStorage.removeItem(draftKey); setDraft(emptyDraft()); await refresh(); await openProject(result.project.projectId) } else setNotice(result.error?.message || 'No se pudo crear la primera versión.'); setBusy(false) }
  if (screen === 'wizard') return <Wizard draft={draft} setDraft={setDraft} notice={notice} setNotice={setNotice} busy={busy} theme={theme} setTheme={setTheme} back={() => setScreen('home')} save={() => { localStorage.setItem(draftKey, JSON.stringify({ ...draft, savedAt: new Date().toISOString() })); setNotice('Borrador guardado localmente.') }} selectAssets={async () => { const result = await getBridge()?.selectInputAssets(); setDraft({ ...draft, assets: (result?.assets || []).map((asset) => ({ safeName: asset.safeName || asset.name || 'material', kind: asset.kind || 'other', sizeBytes: asset.size || 0 })) }) }} create={create} />
  if (screen === 'workspace' && snapshot) return <Workspace snapshot={snapshot} versions={versions} preview={preview} approval={approval} area={area} setArea={setArea} notice={notice} busy={busy} run={run} back={() => setScreen('home')} />
  return <Home projects={projects} draft={draft} setDraft={setDraft} notice={notice} theme={theme} setTheme={setTheme} onCreate={() => { setNotice(''); setScreen('wizard') }} onOpen={openProject} />
}

function Nav({ theme, setTheme, onHome }: { theme: 'light' | 'dark'; setTheme: Dispatch<SetStateAction<'light' | 'dark'>>; onHome?: () => void }) { return <aside className="jefe-nav" aria-label="Navegación principal"><button className="jefe-wordmark" onClick={onHome}><span>J</span> JEFE</button><nav><button className="is-active" onClick={onHome}>Inicio</button><button onClick={onHome}>Proyectos</button><button onClick={onHome}>Orquestador</button><button onClick={onHome}>Versiones</button><button onClick={onHome}>Entregas</button></nav><div className="jefe-nav-footer"><button className="jefe-theme-switch" onClick={() => setTheme(theme === 'light' ? 'dark' : 'light')}>{theme === 'light' ? '☾  Oscuro' : '☀  Claro'}</button><span><i /> Trabajo local</span></div></aside> }

function Home({ projects, draft, setDraft, notice, theme, setTheme, onCreate, onOpen }: { projects: Project[]; draft: Draft; setDraft: Dispatch<SetStateAction<Draft>>; notice: string; theme: 'light' | 'dark'; setTheme: Dispatch<SetStateAction<'light' | 'dark'>>; onCreate: () => void; onOpen: (id: string) => Promise<void> }) {
  const recent = visibleProjects(projects, '', 'recent'); const begin = () => { setDraft({ ...draft, step: 1 }); onCreate() }
  return <main className="jefe-product-shell"><Nav theme={theme} setTheme={setTheme} /><section className="jefe-home-page"><header className="jefe-home-header"><div><span className="jefe-eyebrow">Fábrica local de proyectos</span><p className="jefe-local-status"><i /> Orquestador local listo</p></div><button className="jefe-profile" aria-label="Perfil local">L</button></header><div className="jefe-home-hero"><span className="jefe-eyebrow">Empezá por una idea</span><h1>¿Qué querés<br /><em>construir?</em></h1><p>Contame qué tenés en mente. JEFE lo convierte en un proyecto real, paso a paso y bajo tu control.</p><div className="jefe-idea-box"><label htmlFor="jefe-idea">Tu idea</label><textarea id="jefe-idea" value={draft.need} onChange={(event) => setDraft({ ...draft, need: event.target.value })} placeholder="Ej: una experiencia simple para que mi equipo..." /><div className="jefe-idea-actions"><button type="button" className="jefe-attach" onClick={begin}>＋ Adjuntar materiales</button><span>Tu información queda local</span><button type="button" className="jefe-primary" onClick={begin}>Empezar <span>→</span></button></div></div><div className="jefe-type-picker" aria-label="Tipo de proyecto"><span className="jefe-label">Quiero crear un</span><div>{types.map((option) => <button type="button" key={option.id} className={draft.projectType === option.id ? 'is-selected' : ''} onClick={() => setDraft({ ...draft, projectType: option.id })}><b>{option.glyph}</b><strong>{option.label}</strong></button>)}</div></div></div><section className="jefe-recent-section"><div className="jefe-section-heading"><div><span className="jefe-eyebrow">Tu espacio</span><h2>Proyectos recientes</h2></div><button type="button" onClick={onCreate}>Nuevo proyecto <span>→</span></button></div>{notice && <p className="jefe-notice" aria-live="polite">{notice}</p>}{recent.length ? <div className="jefe-project-list">{recent.slice(0, 4).map((project) => <button className="jefe-project-tile" key={project.projectId} onClick={() => void onOpen(project.projectId)}><span className="jefe-project-mark">{project.projectId.slice(0, 1).toUpperCase()}</span><span><strong>{project.projectId.replace(/-/g, ' ')}</strong><small>{project.projectType} · {project.platform}</small></span><span className="jefe-project-arrow">→</span></button>)}</div> : <div className="jefe-empty-state"><h3>Tu primer proyecto empieza acá.</h3><p>Creá una versión local para verla, pedir cambios y volver a abrirla cuando quieras.</p><button type="button" onClick={onCreate}>Crear primer proyecto</button></div>}</section></section></main>
}

function Wizard({ draft, setDraft, notice, setNotice, busy, theme, setTheme, back, save, selectAssets, create }: { draft: Draft; setDraft: Dispatch<SetStateAction<Draft>>; notice: string; setNotice: (value: string) => void; busy: boolean; theme: 'light' | 'dark'; setTheme: Dispatch<SetStateAction<'light' | 'dark'>>; back: () => void; save: () => void; selectAssets: () => Promise<void>; create: () => Promise<void> }) {
  const labels = ['Proyecto', 'Objetivo e identidad', 'Materiales y referencias', 'Dirección visual', 'Revisar y empezar']; const next = () => { const error = validateStep(draft, draft.step); if (error) { setNotice(error); return }; setNotice(''); setDraft({ ...draft, step: Math.min(5, draft.step + 1) }) }
  return <main className="jefe-product-shell"><Nav theme={theme} setTheme={setTheme} onHome={back} /><section className="jefe-wizard-page"><header className="jefe-wizard-header"><button type="button" onClick={back}>← Volver</button><span>Paso {draft.step} de 5</span><button type="button" onClick={save}>Guardar borrador</button></header><div className="jefe-wizard-progress">{labels.map((label, index) => <span className={index + 1 === draft.step ? 'is-active' : index + 1 < draft.step ? 'is-done' : ''} key={label}><i>{index + 1}</i>{label}</span>)}</div><div className="jefe-wizard-content"><span className="jefe-eyebrow">{labels[draft.step - 1]}</span><h1>{draft.step === 1 ? 'Demos forma a tu idea.' : draft.step === 3 ? 'Todo contexto ayuda.' : labels[draft.step - 1]}</h1>{draft.step === 1 && <div className="jefe-form-grid"><label>Nombre del proyecto<input value={draft.name} onChange={(event) => setDraft({ ...draft, name: event.target.value })} placeholder="Ej: Estudio Norte" /></label><label>¿Qué necesitás construir?<textarea value={draft.need} onChange={(event) => setDraft({ ...draft, need: event.target.value })} placeholder="Contá el problema que querés resolver..." /></label></div>}{draft.step === 2 && <div className="jefe-form-grid"><label>Tipo de negocio<input value={draft.businessType} onChange={(event) => setDraft({ ...draft, businessType: event.target.value })} /></label><label>¿Para quién es?<input value={draft.audience} onChange={(event) => setDraft({ ...draft, audience: event.target.value })} /></label><label>Propuesta<textarea value={draft.proposition} onChange={(event) => setDraft({ ...draft, proposition: event.target.value })} /></label></div>}{draft.step === 3 && <div className="jefe-form-grid"><button type="button" className="jefe-upload-card" onClick={() => void selectAssets()}>＋ <strong>Adjuntar materiales</strong><small>Imágenes, documentos o referencias locales</small></button><label>Colores<input value={draft.colors} onChange={(event) => setDraft({ ...draft, colors: event.target.value })} /></label><label>Notas visuales<textarea value={draft.notes} onChange={(event) => setDraft({ ...draft, notes: event.target.value })} /></label><label>URLs de referencia<input value={draft.urls} onChange={(event) => setDraft({ ...draft, urls: event.target.value })} /></label><p className="jefe-form-note">{draft.assets.length} materiales seleccionados. Las referencias se conservan; no se analizan automáticamente.</p></div>}{draft.step === 4 && <div className="jefe-direction-grid">{(['editorial', 'comercial', 'expresiva'] as const).map((direction) => <button type="button" key={direction} className={draft.direction === direction ? 'is-selected' : ''} onClick={() => setDraft({ ...draft, direction })}><span>{direction === 'editorial' ? 'Aa' : direction === 'comercial' ? '▦' : '✦'}</span><strong>{direction[0].toUpperCase() + direction.slice(1)}</strong><small>{direction === 'editorial' ? 'Jerarquía, detalle y calma.' : direction === 'comercial' ? 'Claridad, energía y acción.' : 'Contraste, ritmo y carácter.'}</small></button>)}</div>}{draft.step === 5 && <div className="jefe-review-card"><h2>{draft.name || 'Tu proyecto'}</h2><p>{draft.projectType} · dirección {draft.direction || 'pendiente'} · {draft.assets.length} materiales</p><p>Se creará una primera versión local. No incluye despliegue, red ni servicios externos.</p></div>}{notice && <p className="jefe-notice" aria-live="polite">{notice}</p>}<div className="jefe-wizard-actions">{draft.step > 1 && <button type="button" onClick={() => { setNotice(''); setDraft({ ...draft, step: draft.step - 1 }) }}>Atrás</button>}{draft.step < 5 ? <button type="button" className="jefe-primary" onClick={next}>Continuar <span>→</span></button> : <button type="button" className="jefe-primary" disabled={busy} onClick={() => void create()}>{busy ? 'Creando…' : 'Crear primera versión'} <span>→</span></button>}</div></div></section></main>
}

function Workspace({ snapshot, versions, preview, approval, area, setArea, notice, busy, run, back }: { snapshot: Snapshot; versions: Version[]; preview: PreviewState | null; approval: ApprovalState | null; area: WorkspaceArea; setArea: (area: WorkspaceArea) => void; notice: string; busy: boolean; run: (work: () => Promise<Result>, success: string) => Promise<void>; back: () => void }) {
  const [change, setChange] = useState(''); const [direction, setDirection] = useState<Direction>('editorial'); const [previewInspected, setPreviewInspected] = useState(false); const [rejectionReason, setRejectionReason] = useState(''); const [left, setLeft] = useState(versions[0]?.versionId || ''); const [right, setRight] = useState(versions.at(-1)?.versionId || ''); const [comparison, setComparison] = useState<Result['comparison']>(); const [reviewed, setReviewed] = useState(false); const view = workspaceView(snapshot, versions); const pBridge = getPreviewBridge()
  const openRealPreview = async () => { if (!pBridge || !preview) return; const result = await pBridge.open(preview.projectId, preview.previewRequestId); if (result.ok) setPreviewInspected(true) }
  const review = async (decision: 'viewed' | 'approved' | 'rejected', reason?: string) => { if (!pBridge || !preview) return; await run(async () => { const result = await pBridge.review({ projectId: preview.projectId, previewRequestId: preview.previewRequestId, decision, ...(reason ? { reason } : {}) }); if (!result.ok || !result.review) return result; setReviewed(true); if (decision === 'approved' || decision === 'rejected') return pBridge.approval({ projectId: preview.projectId, previewRequestId: preview.previewRequestId, reviewId: result.review.reviewId, decision }); return result }, decision === 'viewed' ? 'Preview registrado como visto.' : decision === 'approved' ? 'Aprobación durable registrada.' : 'Rechazo durable registrado.') }
  const tabs: Array<[WorkspaceArea, string]> = [['summary', 'Resumen'], ['build', 'Construir'], ['materials', 'Materiales'], ['versions', 'Versiones y entrega']]
  return <main className="jefe-product-shell jefe-product-shell--workspace"><aside className="jefe-nav"><button className="jefe-wordmark" onClick={back}><span>J</span> JEFE</button><nav><button onClick={back}>Inicio</button><button className="is-active">Proyecto</button><button>Orquestador</button><button>Versiones</button><button>Entregas</button></nav><div className="jefe-nav-footer"><span><i /> Trabajo local</span></div></aside><section className="jefe-workspace-page"><header className="jefe-workspace-header"><div><button className="jefe-back-link" onClick={back}>← Proyectos</button><span className="jefe-eyebrow">Workspace local</span><h1>{snapshot.projectName}</h1><p>{view.state.label} · {snapshot.creativeDirection || 'Dirección pendiente'}</p></div><div className="jefe-workspace-next"><span>Próximo paso</span><strong>{view.state.nextStep}</strong>{view.state.blocker && <small>{view.state.blocker}</small>}</div></header><nav className="jefe-workspace-tabs" role="tablist" aria-label="Áreas del proyecto">{tabs.map(([id, label]) => <button key={id} role="tab" aria-selected={area === id} onClick={() => setArea(id)}>{label}</button>)}</nav>{notice && <p className="jefe-notice" aria-live="polite">{notice}</p>}<div className="jefe-workspace-layout"><section className="jefe-preview-panel"><div className="jefe-panel-heading"><div><span className="jefe-eyebrow">Objeto de revisión</span><h2>Preview real</h2></div><span className="jefe-status-pill">{preview?.state || 'No disponible'}</span></div><div className="jefe-preview-stage"><div className="jefe-preview-browser"><span /><span /><span /><b>app/index.html</b></div><div className="jefe-preview-empty"><span className="jefe-preview-symbol">✦</span><strong>Contenido físico listo para abrir</strong><p>La vista corresponde exactamente a la versión y snapshot seleccionados.</p><button className="jefe-primary" disabled={busy || !preview} onClick={() => void openRealPreview()}>Abrir preview real <span>↗</span></button></div></div>{preview && <div className="jefe-preview-meta"><span>Versión<strong>{preview.versionId}</strong></span><span>Recurso<strong>{preview.resource.relativePath}</strong></span><span>Snapshot<strong>{preview.versionSnapshot.snapshotSha256.slice(0, 16)}…</strong></span></div>}<p className="jefe-context-note">Este contenido físico es el objeto exacto de aprobación. Se abre en una ventana aislada mediante Electron seguro.</p></section><aside className="jefe-review-panel"><span className="jefe-eyebrow">Revisión humana</span><h2>¿Está listo?</h2><p>Primero abrí el preview real. Las acciones aparecen sólo después de inspeccionarlo.</p>{previewInspected && <button type="button" onClick={() => void review('viewed')} disabled={busy || reviewed}>Registrar que fue visto</button>}{previewInspected && (!approval || approval.state === 'pending_review' || approval.state === 'reviewed') && <div className="jefe-decision-actions"><button type="button" className="jefe-primary" disabled={busy} onClick={() => void review('approved')}>Aprobar preview</button><label>Motivo del rechazo<input value={rejectionReason} onChange={(event) => setRejectionReason(event.target.value)} placeholder="Contá qué debería cambiar" /></label><button type="button" className="jefe-danger-button" disabled={busy || !rejectionReason.trim()} onClick={() => void review('rejected', rejectionReason)}>Rechazar preview</button></div>}{approval && <p className="jefe-review-result">Estado durable: <strong>{approval.state}</strong>{approval.terminalReason ? ` · ${approval.terminalReason}` : ''}</p>}</aside></div>{area === 'summary' && <div className="jefe-workspace-columns"><article className="jefe-surface-card"><span className="jefe-eyebrow">En camino</span><h2>Progreso del proyecto</h2><ul className="jefe-progress-list">{view.progress.map((item) => <li key={item.label}><i className={item.done ? 'is-done' : ''}>{item.done ? '✓' : '·'}</i>{item.label}</li>)}</ul></article><article className="jefe-surface-card"><span className="jefe-eyebrow">Actividad</span><h2>Lo que pasó</h2><ol className="jefe-activity-list">{snapshot.events.slice().reverse().slice(0, 5).map((event) => <li key={event.eventId}><small>{formatDate(event.occurredAt)}</small>{eventLabel(event)}</li>)}</ol></article></div>}{area === 'build' && <div className="jefe-workspace-columns"><article className="jefe-surface-card"><span className="jefe-eyebrow">Conversación</span><h2>Pedir un cambio</h2><label>¿Qué querés ajustar?<textarea value={change} onChange={(event) => setChange(event.target.value)} placeholder="Ej: hacé más visible la acción principal..." /></label><label>Dirección visual<select value={direction} onChange={(event) => setDirection(event.target.value as Direction)}><option value="editorial">Editorial</option><option value="comercial">Comercial</option><option value="expresiva">Expresiva</option></select></label><button className="jefe-primary" disabled={busy || !change.trim()} onClick={() => void run(() => getBridge()!.createVersion(snapshot.projectId, change, { visualDirection: direction }), 'Nueva versión local creada.')}>Pedir cambio <span>→</span></button></article><article className="jefe-surface-card"><span className="jefe-eyebrow">Supervisión local</span><h2>El orquestador sigue detrás</h2><p>La ejecución, QA, memoria y persistencia se conservan detrás de esta experiencia.</p></article></div>}{area === 'materials' && <div className="jefe-workspace-columns"><article className="jefe-surface-card"><span className="jefe-eyebrow">Contexto guardado</span><h2>Materiales</h2>{snapshot.materials.files.length ? <ul>{snapshot.materials.files.map((file) => <li key={file.safeName}>{file.safeName} · {file.kind}</li>)}</ul> : <p>No se aportaron materiales.</p>}</article><article className="jefe-surface-card"><span className="jefe-eyebrow">Dirección</span><h2>Notas del proyecto</h2><p>{snapshot.materials.notes || 'Sin notas declaradas.'}</p><p>{snapshot.materials.colors.join(', ') || 'Sin colores declarados.'}</p></article></div>}{area === 'versions' && <div className="jefe-workspace-columns"><article className="jefe-surface-card"><span className="jefe-eyebrow">Historial local</span><h2>Versiones</h2><ol>{versions.map((version, index) => <li key={version.versionId}><strong>{version.versionId === snapshot.activeVersionId ? 'Actual' : `Versión ${index + 1}`}</strong> · {formatDate(version.createdAt)}<button onClick={() => { if (window.confirm('Se creará una nueva versión basada en esta versión.')) void run(() => getBridge()!.restoreVersion(snapshot.projectId, version.versionId), 'Versión restaurada como nueva versión.') }}>Restaurar</button></li>)}</ol></article><article className="jefe-surface-card"><span className="jefe-eyebrow">Entrega</span><h2>Comparar y entregar</h2>{view.hasComparison && <><label>Versión A<select value={left} onChange={(event) => setLeft(event.target.value)}>{versions.map((version, index) => <option key={version.versionId} value={version.versionId}>Versión {index + 1}</option>)}</select></label><label>Versión B<select value={right} onChange={(event) => setRight(event.target.value)}>{versions.map((version, index) => <option key={version.versionId} value={version.versionId}>Versión {index + 1}</option>)}</select></label><button disabled={busy || left === right} onClick={async () => { const result = await getBridge()!.compareVersions(snapshot.projectId, left, right); if (result.ok) setComparison(result.comparison || undefined) }}>Comparar cambios</button>{comparison && <p>{comparison.artifacts.added.length} agregados · {comparison.artifacts.modified.length} modificados · {comparison.artifacts.removed.length} eliminados.</p>}</>}{snapshot.approval.approved ? <button className="jefe-primary" disabled={busy || snapshot.deliveryAvailable} onClick={() => void run(() => getBridge()!.prepareLocalDelivery(snapshot.projectId, snapshot.activeVersionId), 'Entrega local preparada.')}>Preparar entrega</button> : <p>La entrega queda disponible después de la aprobación.</p>}{snapshot.deliveryAvailable && <button onClick={() => void run(() => getBridge()!.openDelivery(snapshot.projectId, snapshot.activeVersionId), 'Entrega local abierta.')}>Abrir entrega</button>}</article></div>}</section></main>
}
