import { FACTORY_HERMES_WRAPPER_NO_TOOL_MODE_IMPLEMENTATION_APPROVAL_KIND, FACTORY_HERMES_WRAPPER_NO_TOOL_MODE_IMPLEMENTATION_APPROVAL_VERSION, WRAPPER_NO_TOOL_MODE_IMPLEMENTATION_APPROVAL_NOT_AUTHORIZED_ACTIONS } from './hermes-wrapper-no-tool-mode-implementation-approval.defaults.ts'
import type { FactoryHermesWrapperNoToolModeImplementationApprovalInput, FactoryHermesWrapperNoToolModeImplementationApprovalResult, FactoryHermesWrapperNoToolModeImplementationApprovalValidationResult } from './hermes-wrapper-no-tool-mode-implementation-approval.types.ts'
const SECRETISH = /(sk-[a-z0-9]|bearer\s+|api[_-]?key\s*[:=]\s*[^,\s]+|password\s*[:=]|token\s*[:=]|process\.env|\.env|full stdout|full stderr|full source)/iu
export function validateFactoryHermesWrapperNoToolModeImplementationApprovalInput(input: FactoryHermesWrapperNoToolModeImplementationApprovalInput): FactoryHermesWrapperNoToolModeImplementationApprovalValidationResult { const errors: string[] = []; const warnings: string[] = []; if (!input?.evaluatedAt) errors.push('evaluatedAt is required.'); if (!input?.evaluatedBy) errors.push('evaluatedBy is required.'); if (!input?.implementationPlanningResult) errors.push('implementation planning result is required.'); if (SECRETISH.test(JSON.stringify({ approvalNotes: input?.approvalNotes }))) errors.push('input appears to contain secret-like values.'); return { ok: errors.length === 0, errors, warnings } }
export function validateFactoryHermesWrapperNoToolModeImplementationApprovalResult(result: FactoryHermesWrapperNoToolModeImplementationApprovalResult): FactoryHermesWrapperNoToolModeImplementationApprovalValidationResult { const errors: string[] = []; const warnings: string[] = []; if (result.approvalKind !== FACTORY_HERMES_WRAPPER_NO_TOOL_MODE_IMPLEMENTATION_APPROVAL_KIND) errors.push('approvalKind mismatch.'); if (result.approvalVersion !== FACTORY_HERMES_WRAPPER_NO_TOOL_MODE_IMPLEMENTATION_APPROVAL_VERSION) errors.push('approvalVersion mismatch.'); if (!['wrapper_no_tool_mode_implementation_approval_granted','wrapper_no_tool_mode_implementation_approval_blocked'].includes(result.status)) errors.push('invalid status.'); if (result.status === 'wrapper_no_tool_mode_implementation_approval_granted' && !result.approvedWrapperNoToolModeImplementationEnvelope) errors.push('approved envelope required.'); if (result.status === 'wrapper_no_tool_mode_implementation_approval_blocked' && !result.approvalBlockerPlan) errors.push('blocker plan required.'); for (const key of ['canProceedToHermesWrapperNoToolModeVerificationPlanning','canProceedToResearchRuntimeAdapterApprovalRetry','canProceedToResearchRuntimeAdapter','canRunResearchNow','canExecuteHermesNow','canPassPromptNow','canUseNetworkNow','canUseCredentialsNow','canReadEnvSecretsNow','canCallModelsNow','canEnableToolsetsNow','canMutateFilesystemNow','canUseFindings'] as const) if (result[key] !== false) errors.push(`${key} must be false.`); for (const action of WRAPPER_NO_TOOL_MODE_IMPLEMENTATION_APPROVAL_NOT_AUTHORIZED_ACTIONS) if (!result.wrapperNoToolModeImplementationApprovalReceipt.notAuthorizedActions.includes(action)) errors.push(`missing notAuthorizedAction: ${action}`); if (SECRETISH.test(JSON.stringify(result))) warnings.push('result contains sensitive-looking label; review before logging.'); return { ok: errors.length === 0, errors, warnings } }
