import { FACTORY_HERMES_RESEARCH_RUNTIME_PLANNING_KIND, FACTORY_HERMES_RESEARCH_RUNTIME_PLANNING_VERSION } from './hermes-research-runtime-planning.defaults.ts'
import type { FactoryHermesResearchRuntimePlanningInput, FactoryHermesResearchRuntimePlanningResult, FactoryHermesResearchRuntimePlanningValidationResult } from './hermes-research-runtime-planning.types.ts'

export function validateFactoryHermesResearchRuntimePlanningInput(input: FactoryHermesResearchRuntimePlanningInput): FactoryHermesResearchRuntimePlanningValidationResult { const errors: string[] = []; const warnings: string[] = []; if (!input.plannedAt) errors.push('plannedAt is required.'); if (!input.plannedBy) errors.push('plannedBy is required.'); if (!input.pythonInstallJefeReviewResult) errors.push('pythonInstallJefeReviewResult is required.'); if (!input.sourceInspection) errors.push('sourceInspection is required.'); if (/secret|token|password|api[_-]?key|credential/iu.test(JSON.stringify(input.planningNotes ?? ''))) errors.push('planningNotes must not contain apparent secrets.'); return { ok: errors.length === 0, errors, warnings } }
export function validateFactoryHermesResearchRuntimePlanningResult(result: FactoryHermesResearchRuntimePlanningResult): FactoryHermesResearchRuntimePlanningValidationResult { const errors: string[] = []; const warnings: string[] = []; if (result.planningKind !== FACTORY_HERMES_RESEARCH_RUNTIME_PLANNING_KIND) errors.push('Invalid kind.'); if (result.planningVersion !== FACTORY_HERMES_RESEARCH_RUNTIME_PLANNING_VERSION) errors.push('Invalid version.'); if (result.toolId !== 'hermes_agent') errors.push('toolId must be hermes_agent.'); if (result.status === 'plan_candidate_created') { if (!result.researchRuntimePlanningReceipt) errors.push('plan candidate requires receipt.'); if (!result.hermesResearchRuntimePlanCandidate) errors.push('plan candidate requires plan.'); if (result.researchRuntimePlanningReceipt?.approvedNextGate !== 'Factory Hermes Research Runtime Boundary Contract v1') errors.push('approvedNextGate must be boundary contract.'); if (result.canProceedToResearchRuntimeBoundary !== true) errors.push('plan candidate requires boundary proceed true.'); } if (result.canExecuteHermes !== false || result.canRunHermesScripts !== false || result.canUseCredentials !== false || result.canCallModels !== false || result.canUseNetwork !== false || result.canMutateProjectFiles !== false || result.canDeploy !== false) errors.push('Dangerous flags must remain false.'); const actions = result.researchRuntimePlanningReceipt?.notAuthorizedActions || []; for (const action of ['execute_hermes_now', 'call_models_now', 'access_credentials_now', 'use_network_now']) if (result.researchRuntimePlanningReceipt && !actions.includes(action)) errors.push(`Missing notAuthorizedAction ${action}.`); if (!result.recommendedNextStep) errors.push('recommendedNextStep is required.'); return { ok: errors.length === 0, errors, warnings } }
