import { FACTORY_HERMES_WRAPPER_NO_TOOL_MODE_PLANNING_KIND, FACTORY_HERMES_WRAPPER_NO_TOOL_MODE_PLANNING_VERSION, WRAPPER_NO_TOOL_MODE_NOT_AUTHORIZED_ACTIONS } from './hermes-wrapper-no-tool-mode-planning.defaults.ts'
import type { FactoryHermesWrapperNoToolModePlanningInput, FactoryHermesWrapperNoToolModePlanningResult, FactoryHermesWrapperNoToolModePlanningValidationResult } from './hermes-wrapper-no-tool-mode-planning.types.ts'
const SECRETISH = /(sk-[a-z0-9]|bearer\s+|api[_-]?key\s*[:=]\s*[^,\s]+|password\s*[:=]|token\s*[:=]|process\.env|\.env|full stdout|full stderr|full source)/iu
export function validateFactoryHermesWrapperNoToolModePlanningInput(input: FactoryHermesWrapperNoToolModePlanningInput): FactoryHermesWrapperNoToolModePlanningValidationResult { const errors: string[] = []; const warnings: string[] = []; if (!input?.plannedAt) errors.push('plannedAt is required.'); if (!input?.plannedBy) errors.push('plannedBy is required.'); if (!input?.runtimeSelectionRevisionPlanningResult) errors.push('runtimeSelectionRevisionPlanningResult is required.'); if (!input?.toolsetDisableVerificationApprovalResult) errors.push('toolsetDisableVerificationApprovalResult is required.'); if (SECRETISH.test(JSON.stringify({ planningNotes: input?.planningNotes }))) errors.push('input appears to contain secret-like values.'); return { ok: errors.length === 0, errors, warnings } }
export function validateFactoryHermesWrapperNoToolModePlanningResult(result: FactoryHermesWrapperNoToolModePlanningResult): FactoryHermesWrapperNoToolModePlanningValidationResult { const errors: string[] = []; const warnings: string[] = []; if (result.planningKind !== FACTORY_HERMES_WRAPPER_NO_TOOL_MODE_PLANNING_KIND) errors.push('planningKind mismatch.'); if (result.planningVersion !== FACTORY_HERMES_WRAPPER_NO_TOOL_MODE_PLANNING_VERSION) errors.push('planningVersion mismatch.'); if (!['wrapper_no_tool_mode_plan_created', 'wrapper_no_tool_mode_plan_blocked'].includes(result.status)) errors.push('invalid status.'); if (!result.wrapperPlanningStatus) errors.push('wrapperPlanningStatus required.'); if (!result.wrapperSourceInspectionMap) errors.push('wrapperSourceInspectionMap required.'); if (!result.wrapperStrategyCandidates?.length) errors.push('strategy candidates required.'); if (!result.wrapperNoToolModeDecisionPack) errors.push('decision pack required.'); if (!result.recommendedWrapperPath) errors.push('recommended path required.'); if (!result.hermesWrapperNoToolModePlanCandidate) errors.push('plan candidate required.'); if (result.hermesWrapperNoToolModePlanCandidate.directHermesRuntimeAdapterBlocked !== true) errors.push('direct adapter must be blocked.'); for (const key of ['sourceMutationAllowedNow', 'wrapperImplementationAllowedNow', 'wrapperExecutionAllowedNow', 'runtimeAdapterAllowedNow', 'researchExecutionAllowedNow'] as const) if (result.hermesWrapperNoToolModePlanCandidate[key] !== false) errors.push(`${key} must be false.`); for (const key of ['canProceedToResearchRuntimeAdapterApprovalRetry', 'canProceedToResearchRuntimeAdapter', 'canRunResearchNow', 'canExecuteHermesNow', 'canPassPromptNow', 'canUseNetworkNow', 'canUseCredentialsNow', 'canReadEnvSecretsNow', 'canCallModelsNow', 'canEnableToolsetsNow', 'canMutateFilesystemNow', 'canUseFindings'] as const) if (result[key] !== false) errors.push(`${key} must be false.`); for (const action of WRAPPER_NO_TOOL_MODE_NOT_AUTHORIZED_ACTIONS) if (!result.wrapperNoToolModePlanningReceipt.notAuthorizedActions.includes(action)) errors.push(`missing notAuthorizedAction: ${action}`); if (SECRETISH.test(JSON.stringify({ receipt: result.wrapperNoToolModePlanningReceipt }))) errors.push('result appears to contain secret-like values.'); return { ok: errors.length === 0, errors, warnings } }
