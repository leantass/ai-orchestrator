import { FACTORY_HERMES_WRAPPER_NO_TOOL_MODE_IMPLEMENTATION_PLANNING_KIND, FACTORY_HERMES_WRAPPER_NO_TOOL_MODE_IMPLEMENTATION_PLANNING_VERSION, WRAPPER_NO_TOOL_MODE_IMPLEMENTATION_PLANNING_NOT_AUTHORIZED_ACTIONS } from './hermes-wrapper-no-tool-mode-implementation-planning.defaults.ts'
import type { FactoryHermesWrapperNoToolModeImplementationPlanningInput, FactoryHermesWrapperNoToolModeImplementationPlanningResult, FactoryHermesWrapperNoToolModeImplementationPlanningValidationResult } from './hermes-wrapper-no-tool-mode-implementation-planning.types.ts'
const SECRETISH = /(sk-[a-z0-9]|bearer\s+|api[_-]?key\s*[:=]\s*[^,\s]+|password\s*[:=]|token\s*[:=]|process\.env|\.env|full stdout|full stderr|full source)/iu
export function validateFactoryHermesWrapperNoToolModeImplementationPlanningInput(input: FactoryHermesWrapperNoToolModeImplementationPlanningInput): FactoryHermesWrapperNoToolModeImplementationPlanningValidationResult { const errors: string[] = []; const warnings: string[] = []; if (!input?.plannedAt) errors.push('plannedAt is required.'); if (!input?.plannedBy) errors.push('plannedBy is required.'); if (!input?.wrapperNoToolModeApprovalResult) errors.push('wrapper approval result is required.'); if (SECRETISH.test(JSON.stringify({ planningNotes: input?.planningNotes }))) errors.push('input appears to contain secret-like values.'); return { ok: errors.length === 0, errors, warnings } }
export function validateFactoryHermesWrapperNoToolModeImplementationPlanningResult(result: FactoryHermesWrapperNoToolModeImplementationPlanningResult): FactoryHermesWrapperNoToolModeImplementationPlanningValidationResult { const errors: string[] = []; const warnings: string[] = []; if (result.planningKind !== FACTORY_HERMES_WRAPPER_NO_TOOL_MODE_IMPLEMENTATION_PLANNING_KIND) errors.push('planningKind mismatch.'); if (result.planningVersion !== FACTORY_HERMES_WRAPPER_NO_TOOL_MODE_IMPLEMENTATION_PLANNING_VERSION) errors.push('planningVersion mismatch.'); if (result.status !== 'wrapper_no_tool_mode_implementation_plan_created') errors.push('status must be wrapper_no_tool_mode_implementation_plan_created.'); if (result.decision !== 'hermes_wrapper_no_tool_mode_implementation_plan_created_for_approval') errors.push('decision mismatch.'); for (const key of ['wrapperImplementationArchitecturePlan','wrapperTempConfigPlan','wrapperNoToolEnforcementPlan','wrapperValidationPlan','wrapperImplementationRiskRegister','wrapperNoToolModeImplementationPlanningReceipt','hermesWrapperNoToolModeImplementationPlanCandidate'] as const) if (!result[key]) errors.push(`${key} required.`); for (const key of ['canProceedToHermesWrapperNoToolModeImplementation','canProceedToResearchRuntimeAdapterApprovalRetry','canProceedToResearchRuntimeAdapter','canRunResearchNow','canExecuteHermesNow','canPassPromptNow','canUseNetworkNow','canUseCredentialsNow','canReadEnvSecretsNow','canCallModelsNow','canEnableToolsetsNow','canMutateFilesystemNow','canUseFindings'] as const) if (result[key] !== false) errors.push(`${key} must be false.`); if (result.canProceedToHermesWrapperNoToolModeImplementationApproval !== true) errors.push('canProceedToHermesWrapperNoToolModeImplementationApproval must be true.'); for (const action of WRAPPER_NO_TOOL_MODE_IMPLEMENTATION_PLANNING_NOT_AUTHORIZED_ACTIONS) if (!result.wrapperNoToolModeImplementationPlanningReceipt.notAuthorizedActions.includes(action)) errors.push(`missing notAuthorizedAction: ${action}`); if (SECRETISH.test(JSON.stringify(result))) warnings.push('result contains sensitive-looking label; review before logging.'); return { ok: errors.length === 0, errors, warnings } }
