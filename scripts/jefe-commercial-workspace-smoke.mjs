import assert from 'node:assert/strict'
import { commercialState, eventLabel, workspaceView } from '../src/commercial/hubModel.ts'
const base = { projectId: 'internal-id', projectName: 'Marca', state: 'not_ready', versionCount: 0, activeVersionId: 'internal-version', previewAvailable: false, previewMode: 'unavailable', deliveryAvailable: false, delivery: { status: 'not_ready', deliveredAt: null }, approval: { approved: false, approvedAt: null, observation: null }, nextStep: 'review_and_approve', blockers: ['approval_required'], lastActivity: '2026-01-01T00:00:00.000Z', generationProfile: 'commercial_site', projectType: 'agency_site', platform: 'web', creativeDirection: 'editorial', materials: { totalFiles: 0, references: 0, files: [], colors: [], notes: null, urls: [] }, milestones: { brief: true, materials: false, direction: true, version: false, approved: false, delivery: false }, capabilities: { createVersion: true, approval: false, comparison: false, restore: false, delivery: false }, events: [] }
const zero = workspaceView(base, []); assert.equal(zero.state.label, 'En preparación'); assert.equal(zero.actions.preview, false); assert.equal(zero.hasComparison, false); assert.equal(Object.values(zero).join(' ').includes('internal-id'), false)
const one = { ...base, versionCount: 1, previewAvailable: true, milestones: { ...base.milestones, version: true }, capabilities: { ...base.capabilities, approval: true, restore: true } }; const oneView = workspaceView(one, [{ versionId: 'internal-version', createdAt: '2026-01-01T00:00:00.000Z', summary: 'Inicial', changeOrigin: { kind: 'commercial', reference: null } }]); assert.equal(commercialState(one).label, 'Pendiente de revisión'); assert.equal(oneView.actions.approve, true); assert.equal(oneView.currentVersion?.summary, 'Inicial')
const many = { ...one, versionCount: 2, approval: { approved: true, approvedAt: '2026-01-02T00:00:00.000Z', observation: null }, deliveryAvailable: true, delivery: { status: 'delivered_local', deliveredAt: '2026-01-02T00:00:00.000Z' }, milestones: { ...one.milestones, approved: true, delivery: true }, capabilities: { ...one.capabilities, comparison: true, delivery: true }, events: [{ eventId: 'event-000001', sequence: 1, type: 'version_approval_changed', occurredAt: '2026-01-02T00:00:00.000Z', approved: true }] }; const manyView = workspaceView(many, [{ versionId: 'internal-version', createdAt: '2026-01-01T00:00:00.000Z', summary: 'Inicial', changeOrigin: { kind: 'commercial', reference: null } }, { versionId: 'internal-version-2', createdAt: '2026-01-02T00:00:00.000Z', summary: 'Cambio', changeOrigin: { kind: 'commercial', reference: null } }]); assert.equal(manyView.state.label, 'Entrega local disponible'); assert.equal(manyView.actions.delivery, true); assert.equal(manyView.hasComparison, true); assert.equal(eventLabel(many.events[0]), 'Versión aprobada por el usuario'); assert.equal(manyView.progress.filter((item) => item.done).length, 5)
console.log('PASS jefe-commercial-workspace-smoke')
